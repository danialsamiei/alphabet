/**
 * @module @awaf/ui
 * @description
 * پکیج UI — ۵ لایه UI Degradation + React hooks + components.
 * UI package — 5-layer degradation + React hooks + components.
 * (Phase 1 stub — implementation in Phase 2)
 */

export type { CapabilityLayer } from '@awaf/core';
