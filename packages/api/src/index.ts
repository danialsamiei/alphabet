/**
 * @module @awaf/api
 * @description
 * پکیج API Client — ۱۶ endpoint client + SSE streaming.
 * API Client package — 16 endpoint clients + SSE streaming.
 * (Phase 1 stub — implementation in Phase 2)
 */

export type { AWAFRequest, AWAFResponse } from '@awaf/core';
